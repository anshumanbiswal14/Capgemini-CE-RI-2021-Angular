import { StringpipePipe } from './stringpipe.pipe';

describe('StringpipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringpipePipe();
    expect(pipe).toBeTruthy();
  });
});
